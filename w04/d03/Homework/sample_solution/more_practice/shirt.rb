#this shirt class is completely useless because
#it takes attributes but doesn't provide any
#methods to manipulate or operate on those attributes

class Shirt
  def initialize(size, color, text)
    @size = size
    @color = color
    @text = text
  end
end
